# Auth & roles

TODO: fill when working that area.

Guards, role flags, and portal access patterns for CRM/HRM. Point to code (`auth-guard`, middleware) instead of inventing a second source of truth.
